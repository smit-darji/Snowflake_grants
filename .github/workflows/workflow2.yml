name: Workflow 2

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      # Add your build steps here
      - name: Run build
        run: cat /proc/cpuinfo | grep 'cpu core'
