name: Workflow 1

on:
  push:
    branches:
      - Master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      # Add your build steps here
      - name: Run build
        run: echo "Running build for Workflow 1"

      # Add a 60-second wait time
      - name: Wait for 60 seconds
        run: sleep 60

